# Autogenerated bulkloader.yaml file.
# You must edit this file before using it. TODO: Remove this line when done.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users
- import: transforms.transforms

transformers:

- kind: OilGasLease
  connector: csv
  connector_options:
    columns: from_header
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.create_foreign_key('OilGasLease', key_is_id=True)

    - property: activity
      external_name: Activity
      # Type: String Stats: 1 properties of this type in this kind.

    - property: bookPage
      external_name: BookPage
      # Type: String Stats: 1 properties of this type in this kind.

    - property: cessationDays
      external_name: CessationDays
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: cessationOfProduction
      external_name: CessationOfProduction
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: companySource
      external_name: CompanySource
      # Type: String Stats: 1 properties of this type in this kind.

    - property: county
      external_name: County
      # Type: String Stats: 1 properties of this type in this kind.

    - property: depthClauseAdjustment
      external_name: DepthClauseAdjustment
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: depthLimitation
      external_name: DepthLimitation
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: extensionMonths
      external_name: ExtensionMonths
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: favoredNations
      external_name: FavoredNations
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: grossProceeds
      external_name: GrossProceeds
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: instrumentType
      external_name: InstrumentType
      # Type: String Stats: 1 properties of this type in this kind.

    - property: otherProvisions
      external_name: OtherProvisions
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: prefRightsDaysPastTerm
      external_name: PrefRightsDaysPastTerm
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: prefRights
      external_name: PrefRights
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: pughClause
      external_name: PughClause
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: shutInRoyaltiesMonths
      external_name: ShutInRoyaltiesMonths
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: shutInRoyalties
      external_name: ShutInRoyalties
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: subjectToPooling
      external_name: SubjectToPooling
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: surfaceProvisions
      external_name: SurfaceProvisions
      # Type: Boolean Stats: 1 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: termOptionMonths
      external_name: TermOptionMonths
      # Type: Integer Stats: 1 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property:  parentOGL
      external_name: ParentOilAndGasLease
      export_transform: transform.key_id_or_name_as_string
      import_transform: transform.none_if_empty(transform.create_foreign_key('OilGasLease', key_is_id=True))

    - property: depthClauseType
      external_name: DepthClauseType
      
    - property: documentDate
      external_name: DocumentDate
      #export_transform: transform.export_date_time('%m/%d/%Y')
      #import_transform: transform.import_date_time('%m/%d/%Y')
      
    - property: effectiveDate
      external_name: EffectiveDate
      #export_transform: transform.export_date_time('%m/%d/%Y')
      #import_transform: transform.import_date_time('%m/%d/%Y')
      
    - property: extensionBonus
      external_name: ExtensionBonus
      import_transform: transform.none_if_empty(transforms.import_currency)
      #import_transform: transform.none_if_empty(float)
    
    - property: filingDate
      external_name: FilingDate
      export_transform: transform.export_date_time('%m/%d/%Y')
      import_transform: transform.import_date_time('%m/%d/%Y')
     
    - property: gross_ac
      external_name: GrossAc
      import_transform: transform.none_if_empty(float)
      
    - property: lessee
      external_name: Lessee
     
    - property: lessor
      external_name: Lessor
    
    - property: royalty
      external_name: Royalty
      import_transform: transform.none_if_empty(float)
      
    - property: state
      external_name: State
      
    - property: subjectToPoolingOrderNo
      external_name: SubjectToPoolingOrderNo
     
    - property: termMonths
      external_name: TermMonths
      import_transform: transform.none_if_empty(int)
     
    

- kind: Tract
  connector: csv
  connector_options:
    columns: from_header
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: oilGasLease
      external_name: OilGasLease
      # Type: Key Stats: 1 properties of this type in this kind.
      import_transform: transform.create_foreign_key('OilGasLease', key_is_id=True)
      export_transform: transform.key_id_or_name_as_string
        
    - property: rng
      external_name: Rng
      # Type: String Stats: 1 properties of this type in this kind.

    - property: sec
      external_name: Sec
      # Type: String Stats: 1 properties of this type in this kind.

    - property: twn
      external_name: Twn
      # Type: String Stats: 1 properties of this type in this kind.

    - property: county
      external_name: County
     
    - property: legal
      external_name: Legal
    
    - property: meridian
      external_name: Meridian
      
    - property: quarter
      external_name: Quarter
      #import_transform: transform.none_if_empty(string)